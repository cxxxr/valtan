#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:clscript) :silent t)
  )

(defpackage :ros.script.clscript-build.3765950064
  (:use :cl))
(in-package :ros.script.clscript-build.3765950064)

(defun main (&rest argv)
  (declare (ignorable argv))
  (compiler::build-system (first argv))
  (fresh-line))
;;; vim: set ft=lisp lisp:
